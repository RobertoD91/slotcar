# --- Firmware compilato dalla CI (non va committato) ------------------------
web/esp32-installer/firmware/*.bin

# --- PlatformIO / ESP32 ------------------------------------------------------
.pio/
esp32/.pio/
.pioenvs/
.piolibdeps/

# --- Segreti: .env NON va mai committato (l'esempio sì) ----------------------
.env
esp32/.env
*.env
!.env.example
!*.env.example

# --- Editor / OS -------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
*~

# --- Python / Node ------------------------------------------------------------
__pycache__/
*.pyc
.pytest_cache/
node_modules/

# --- Export / log --------------------------------------------------------------
*.csv
*.log

# --- Denylist personale per lo scanner segreti (resta sulla tua macchina) ----
tools/secrets-denylist.local.txt

# Artefatto di build: lo riempie esp32/scripts/copy_webapp.py ad ogni `pio run`.
# La sorgente e` web/, qui ci finisce solo una copia per LittleFS.
esp32/data/
